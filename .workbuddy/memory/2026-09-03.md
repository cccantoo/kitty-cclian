# 2026-09-03

## 记账类型切换样式 bug（已修复，随多账本一起提交 346f966）
- 根因：type-toggle 按钮 HTML 只有 `data-type`，而旧 CSS 用 `.type-toggle button.active.expense/.income/.transfer` 匹配——`.income/.transfer` 类从未被添加，只有硬编码了 `expense` 类的「支出」有样式。
- 修复：CSS 改为 `.active[data-type="expense"/"income"/"transfer"]`（工作区原已有人改，本次一并清理 HTML 里多余 `expense` 类）。
- 部署缓存坑：nginx 对 css/js `immutable` 长缓存 + SW cache-first → 不升版本手机上永远旧样式。本次 SW `kitty-ledger-v14→v15`，index 里 css/js 全部加 `?v=15`（sw.js ASSETS 也同步带 ?v=15）。

## 新功能：多账本（v0.3.5）
- 数据层（db.js）：`kitty_books`(localStorage) 账本数组，账户内嵌账本（accounts）；默认账本 `book-default`（首启从旧 `kitty_accounts` 迁移）。预算 key `kitty_budgets_<bookId>`（旧 `kitty_budgets` 自动迁默认账本）。交易加 `bookId` 字段，历史无 bookId 记录一律算默认账本。KLDB 新导出：books/saveBooks/currentBook/activeBookId/setActiveBookId/loadBudgets/saveBudgets/removeBudgets/cloneDefaultAccounts/DEFAULT_BOOK_ID。
- UI（app.js + index.html + style.css）：记账页顶部账本切换条 → 底部弹层（book-sheet，z140 低于 modal150）：点击行切换 ✓、行尾 ⋯ 进入管理（设为默认/重命名换图标/删除-默认及最后一个不可删）；`＋ 新建账本` 弹窗选 emoji 图标(20个 BOOK_ICONS)+名称，自动克隆默认账户组并切换过去。
- 隔离：state.txs 只装当前账本 → 列表/报表/预算/汇总天然隔离；AI 通道（ai.js）记账/查账/统计/预算全带 `KLDB.activeBookId()`，system prompt 注入当前账本名+该账本账户。
- 导入导出升级 version 2（books+各账本 accounts/budgets+全量交易）；v1 备份兼容：无 bookId 交易归默认账本、旧 budgets 入默认账本。清空数据会连账本一起重置回默认。
- 只读校验：`node --check` 4 文件通过；静态 id、函数重复、CSS 括号均已核对；未做真实浏览器测试（agent-browser 未安装，需 500MB Chromium，跳过）。

## 部署状态（重要）
- 已 commit `346f966`（含之前 e9f8831 共 2 个提交待推送），本地 8444 服务实时生效。
- git push 被卡：`credential.helper=helper-selector` 需交互 TTY，非交互环境挂起返回 exit1 空输出（沙箱内外都一样）。已确认代理 7897 正常、ls-remote 通、无 env token/无 gh/无 .git-credentials。
- 待办：用户手动 `git push origin main`（首次需 GitHub 授权一次），然后服务器 `/var/www/kitty-cclian` `git pull`；手机端 PWA 需重新打开 1-2 次以触发 SW v15 更新。
